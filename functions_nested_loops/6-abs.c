#include "main.h"
/**
 * _abs - calculate the absolute value of the number from zero.
 *@y: the integer to be converted.
 * Return: absolute value of the input number.
 */
int _abs(int y)
{
	if (y > 0)
		return (y);
	else
		return (-y);

}
